# แผนแตก `sc_opex` ออกจาก key-value store

เขียน 2026-09-08 · **ทำถึงขั้นที่ 2 แล้ว** (ขั้น 0–2 เสร็จ · ขั้น 3 backfill เป็นขั้นถัดไป)

> **อ่าน "กฎทางธุรกิจที่ต้องไม่ละเมิด" ใน `CLAUDE.md` ก่อนเริ่มทุกครั้ง** ถ้าขัดกันให้ยึด `CLAUDE.md`

---

## ทำไมต้องแตก

`sc_opex` เป็นตารางเดียวที่เก็บของ **20 รูปแบบ** ปนกัน แยกกันด้วย `category`/`key` ที่เป็น
free-text ไม่มี enum บังคับที่ระดับฐานข้อมูล — บั๊กเรื่องเงินทุกตัวที่เจอมามีต้นตอเดียวกันหมด:

| บั๊กที่เคยเกิด | เกิดเพราะ |
|---|---|
| หน้าภาพรวมนับค่าใช้จ่ายขาด ฿6,600.51/เดือน | whitelist ชื่อหมวดที่ผู้ใช้ตั้งเองได้ |
| รายรับห้องเช่าถูกนับเป็นรายจ่าย | filter เช็คชื่อ category ผิดตัว (`rental_meter` vs `rental_income`) |
| เงินเดือนถูกนับซ้ำ ~฿27,275 | แถว `emp_*` ผ่าน filter หลักเข้าไปนับใน OPEX ด้วย |
| ลบรายการย่อยเบ็ดเตล็ดไม่ได้ | id สังเคราะห์ `"<rowId>-misc-<idx>"` เทียบกับ `id` ที่เป็น bigint |
| timestamp โผล่ในคอลัมน์ `amount` | ไม่มี constraint กันช่วงค่า |
| ค่าที่ปรึกษา = เงินเดือนหุ้นส่วน ที่ตั้งชื่อคนละอย่างทุกเดือน | `name` เป็น free-text ไม่มีตัวอ้างอิงพนักงานจริง |

## รูปแบบทั้งหมดที่อยู่ใน `sc_opex` วันนี้ (385 แถว · 20 รูปแบบ)

| category | รูปแบบ key | แถว | มียอดเงิน | ปลายทางที่ควรไป |
|---|---|---|---|---|
| `payslip_detail` | `empd_*` | 118 | 27 | ตารางสลิปเงินเดือน |
| `rental_meter` | `room_*` | 60 | 12 | ตารางห้องเช่า |
| `น้ำยา & วัสดุสิ้นเปลือง` | `custom_*` | 36 | 36 | **ตารางค่าใช้จ่าย** |
| `ค่าแรงพนักงาน` | `emp_*` | 21 | 19 | ตารางสลิปเงินเดือน (ยอดสุทธิ) |
| `rental_income` | `room_*` | 21 | 12 | ตารางห้องเช่า (เป็น**รายรับ**) |
| `payslip_detail` | `empd_deduct_json_*` | 20 | 0 | ตารางรายการหักของสลิป |
| `ดำเนินงาน & เบ็ดเตล็ด` | `custom_*` | 13 | 13 | **ตารางค่าใช้จ่าย** |
| `payslip_detail` | `misc_items_json` | 10 | 0 | **ตารางค่าใช้จ่าย** (แตก JSON เป็นแถว) |
| `ค่าดำเนินการ` | `misc` | 10 | 10 | ทิ้ง — เป็นยอดสรุปซ้ำของ `misc_items_json` |
| `ค่าดำเนินการ` | `rent` / `water` / `internet` / `electricity` | 37 | 37 | **ตารางค่าใช้จ่าย** |
| `payslip_detail` | `audit_log` | 10 | 0 | ⚠️ **ห้ามแตะ** — ระบบเดิมอ่านอยู่ (ดูข้อจำกัดด้านล่าง) |
| `ภาษี` | `withholding_tax` / `sso_employee` / `sso_employer` | 24 | 14 | **ตารางค่าใช้จ่าย** |
| `สาธารณูปโภค & ค่าเช่า` | `custom_*` | 3 | 3 | **ตารางค่าใช้จ่าย** |
| `payslip_detail` | `empd_profile_*` | 1 | 0 | `sc_employees` (ข้อมูลพนักงาน ไม่ผูกกับเดือน) |
| `ค่าแรง & เงินเดือน` | `custom_*` | 1 | 1 | **ตารางค่าใช้จ่าย** |

## 🚧 ข้อจำกัดที่ตัดสินรูปร่างของแผนนี้ทั้งหมด

**`sc_opex` ยังมีผู้ใช้อีกรายที่ไม่ได้อยู่ใน repo นี้: หน้าการเงินของระบบเดิม**
`legacy/sneakercare_dashboard.html` (ยังเป็น production ที่ใช้ทุกวัน) **อ่านและเขียน `sc_opex`
โดยตรงผ่าน Google Apps Script** — เห็นได้ชัดที่บรรทัด 4130 (อ่านแถว `key === 'audit_log'`)
และ 4825 (เขียนแถวนั้นกลับ)

⇒ **ห้าม drop / rename / เปลี่ยนความหมายของคอลัมน์ใน `sc_opex` จนกว่าหน้าการเงินของระบบเดิม
จะถูกแทนที่ครบ** ไม่งั้นร้านจะบันทึกเงินไม่ได้ทันทีในวันที่ deploy และ repo นี้แก้ฝั่ง GAS ไม่ได้
(ไม่ใช่ source of truth ของมัน)

**สรุป: แผนนี้ต้องเป็นการ "เพิ่มตารางใหม่ข้างๆ" ไม่ใช่ "ย้ายแล้วทิ้งของเดิม"**

---

## ขั้นตอน (ทำทีละขั้น หยุดตรวจทุกขั้น)

### ✅ ขั้น 0 — ล็อกสภาพปัจจุบันไว้ก่อน *(เสร็จ 2026-09-08)*
`npm run test:reconcile` → `scripts/test-monthly-reconciliation.mjs`
ยืนยันว่ากำไรสุทธิรายเดือนบน production ยังตรงกับ Excel ที่เจ้าของกระทบยอดด้วยมือทุกบาท
(ก.พ. 3,023.36 · มี.ค. −14,075.62 · เม.ย. 1,962.69 · พ.ค. 11,066.07 · มิ.ย. 14,351.66
· ก.ค. 21,079.52 · ส.ค. 24,524.79) — ระบุเกณฑ์รายรับของแต่ละเดือนไว้ด้วย (ก.ค. ตามบิล · ส.ค. เงินเข้าจริง)

**ต้องรันทุกครั้งก่อนและหลังทุกขั้นที่เหลือ** · อ่าน production จริงจึงไม่อยู่ใน CI
**ห้ามแก้ตัวเลขอ้างอิงในไฟล์เพื่อให้เทสต์ผ่าน** — ถ้าแดงแปลว่ามีอะไรเพี้ยน ต้องหาสาเหตุก่อน

### ✅ ขั้น 1 — ตารางใหม่ `sc_expense_entries` *(เสร็จ 2026-09-08 · รอ apply)*

ไฟล์: `supabase/migrations/0024_sc_expense_entries.sql`
· rollback: `supabase/migrations/rollback/0024_rollback.sql`
· เทสต์: `scripts/test-migration-0024.mjs` (อยู่ใน `npm run test:migration` แล้ว) **ผ่าน 27 ข้อ**
รวมทั้งพิสูจน์ว่า constraint กันของผิดได้จริง (timestamp ในช่องเงิน · ยอด 0/ติดลบ · ชื่อว่าง ·
หมวดที่พิมพ์เอาเอง · backfill ซ้ำ · ของคลังชิ้นเดียวผูกสองแถว) และ rollback ลบได้หมดจริง

**✅ apply บน production แล้ว 2026-09-08** — ตรวจซ้ำกับของจริงว่า: หมวด seed ครบ 7 หมวด ·
constraint ปฏิเสธ timestamp/ยอด 0/หมวดที่พิมพ์เอาเองได้จริง · `anon` ยิงได้ 401 ทั้งสองตาราง ·
`sc_opex` ไม่ถูกแตะเลย (385 แถวเท่าเดิม)

<details><summary>สเปกที่ใช้ (เก็บไว้อ้างอิง)</summary>
```sql
create table public.sc_expense_entries (
  id            bigserial primary key,
  entry_date    date        not null,                  -- วันที่จ่ายจริง ไม่ใช่ "MM/YYYY"
  amount        numeric(12,2) not null check (amount > 0 and amount < 10000000),
  category      text        not null references public.sc_expense_categories(key),
  title         text        not null,
  pay_method    text        not null default 'บัญชีร้าน',
  branch_id     uuid        references public.inv_branches(id),   -- กฎข้อ 11/12
  stock_txn_id  uuid        references public.inv_stock_transactions(id), -- ผูกกับของที่รับเข้าคลัง
  legacy_opex_id bigint,                               -- ตามรอยกลับไปแถวเดิมได้
  created_by    uuid        references public.profiles(id),
  created_at    timestamptz not null default now()
);
```
- `sc_expense_categories` เป็นตารางอ้างอิง (ไม่ใช่ enum จริง เพื่อให้เพิ่มหมวดได้โดยไม่ต้อง migrate)
  seed จาก `lib/expense-categories.ts` ให้ตรงกัน **ห้ามให้ทั้งสองฝั่งหลุดจากกัน**
- `stock_txn_id` คือคำตอบถาวรของปัญหา "เงินอยู่ใน ledger สองสายที่ไม่คุยกัน" —
  ของที่ซื้อเข้าคลังจะผูกกับค่าใช้จ่ายของมันตรงๆ แทนที่จะต้องมีสคริปต์คอยไล่เทียบ
- RLS: `select/insert/update` เฉพาะ `admin`/`co-admin` ผ่าน `sc_get_my_role()` (แบบเดียวกับ `0017`)
  · `delete` เฉพาะ `admin` · `revoke all from anon` เป็นด่านที่สอง (บทเรียนจาก `0016`)

</details>

### ✅ ขั้น 2 — เขียนสองที่ (dual-write) *(เสร็จ 2026-09-08)*

- `lib/expense-mirror.ts` — `mirrorExpenseEntry()` / `unmirrorExpenseEntry()`
  **จงใจไม่ throw เด็ดขาด**: `sc_opex` ยังเป็นแหล่งข้อมูลจริง ถ้ากระจกเงาพลาดต้องไม่ทำให้
  การบันทึกของผู้ใช้ล้มตาม แต่ต้องดังในล็อกเซิร์ฟเวอร์ (บทเรียนจาก `lib/audit.ts`
  ที่เคยกลืน error จนระบบ audit ไม่ทำงานหลายเดือนโดยไม่มีใครรู้)
- `categoryKeyFor()` ใน `lib/expense-categories.ts` แปลง `shortLabel` ที่ `sc_opex` เก็บ
  → `key` ที่ตารางใหม่ต้องการ (มีเทสต์ 8 เคสใน `npm run test:expenses` ข้อ [10])
- ต่อเข้ากับ `addExpense()` และ `deleteExpense()` แล้ว · **ยังไม่เปลี่ยนการอ่าน**
  หน้าเว็บทุกหน้ายังอ่านจาก `sc_opex` เหมือนเดิม ผู้ใช้ไม่เห็นความเปลี่ยนแปลงใดๆ
- `npm run check:expense-mirror` เทียบ **ทีละแถว** (ไม่ใช่แค่ยอดรวม เพราะยอดรวมที่บังเอิญ
  เท่ากันปิดบังการจับคู่ผิดได้) · ล้มเหลวเฉพาะเมื่อ "ยอดไม่ตรง" หรือ "กระจกเงาค้าง"
  ส่วน "ยังไม่มีกระจกเงา" เป็นเรื่องปกติจนกว่าจะทำขั้นที่ 3
- **ทดสอบทั้งวงจรบน production จริงแล้ว**: เขียน `sc_opex` → กระจกเงาไปตารางใหม่ →
  เขียนซ้ำโดนปฏิเสธด้วย unique violation → ลบแล้วหายครบทั้งสองฝั่ง ไม่เหลือขยะ
- `npm run gen:types` (ใหม่) — regenerate `database.types.ts` จาก production ผ่าน VPS
  ในคำสั่งเดียว เก็บ header ที่เขียนมือไว้ครบ **ต้องรันทุกครั้งที่เพิ่มตาราง/คอลัมน์ใหม่**

### 🟡 ขั้น 3 — backfill ข้อมูลเก่า *(โค้ดพร้อมและ dry-run ผ่านแล้ว · รอ apply migration 0025)*

**ต้อง apply `supabase/migrations/0025_expense_entries_legacy_ref.sql` ก่อนถึงจะรันได้**

ทำไมต้องมี 0025: `legacy_opex_id` (bigint) จับคู่ได้แค่ "หนึ่งแถว sc_opex ต่อหนึ่งค่าใช้จ่าย"
แต่รายจ่ายเบ็ดเตล็ดทั้งเดือนถูกยัดเป็น JSON array ก้อนเดียวในแถวเดียว ⇒ ค่าใช้จ่าย N รายการ
ต้องชี้กลับไปแถวเดียวกันหมด ซึ่งชน unique index · แก้ด้วยคีย์ข้อความ `legacy_ref`
(`"<id>"` หรือ `"<id>-misc-<index>"` — รูปแบบเดียวกับ id สังเคราะห์ที่หน้า /expenses ใช้อยู่แล้ว)
พร้อม **trigger เติมให้เองเมื่อโค้ดไม่ได้ส่งมา** เพราะกติกาที่บังคับได้ที่ฐานข้อมูล
ไม่ควรปล่อยให้เป็นหน้าที่ของโค้ดที่วันหนึ่งจะมีคนลืม

สคริปต์: `node scripts/backfill-expense-entries.mjs` (ดูอย่างเดียว) / `--apply` (ลงมือจริง)
**ไม่ลบหรือแก้อะไรใน `sc_opex` เลย — คัดลอกอย่างเดียว** · รันซ้ำได้ปลอดภัย (unique `legacy_ref`)

**ผล dry-run (ตรวจกับ production จริง):** 184 รายการ · ฿457,496.40 · 11 เดือน
และ **พิสูจน์แล้วว่ายอดที่จะย้ายเท่ากับ `calculateExpenseBreakdown().totalOpex` ของสูตรจริง
ทุกเดือนพอดีทุกบาท** (11/2568 – 09/2569) ⇒ ขั้นที่ 4 จะได้ตัวเลขเดิมเป๊ะ

⚠️ **ขอบเขตคือฝั่ง OPEX เท่านั้น** — เงินเดือน (`emp_*`) และห้องเช่า (`room_*`) ยังอยู่ที่
`sc_opex` จนถึงขั้นที่ 5 ⇒ ตอนสลับการอ่าน (ขั้นที่ 4) ต้องเอา opex จากตารางใหม่
**บวก** payroll จาก `sc_opex` มารวมกัน

### ⬜ ขั้น 4 — สลับการอ่าน *(1 วัน)*
- `calculateExpenseBreakdown()` รับข้อมูลจาก `sc_expense_entries` แทน แล้วลบตรรกะ
  "เดาว่าแถวนี้คืออะไร" ทั้งหมดทิ้ง (blacklist หมวด · `isPayslipInternalRow` · `MAX_REASONABLE_AMOUNT`
  — ไม่จำเป็นอีกเพราะ constraint อยู่ที่ฐานข้อมูลแล้ว)
- ยังคง dual-write ต่อไปเพื่อให้ระบบเดิมใช้งานได้

### ⬜ ขั้น 5 — แยกเงินเดือนและห้องเช่าออก *(หลังจากขั้น 4 นิ่งแล้วอย่างน้อย 1 เดือน)*
- `sc_payslips` + `sc_payslip_deductions` (แทน `emp_*` / `empd_*` / JSON สองรูปแบบ)
- `sc_rental_records` (แทน `room_*` ทั้ง `rental_income` และ `rental_meter`)

### ⬜ ขั้น 6 — เลิกใช้ `sc_opex` *(ทำได้ต่อเมื่อหน้าการเงินของระบบเดิมถูกแทนที่แล้วเท่านั้น)*
- ก่อนถึงขั้นนี้ต้องยืนยันว่า `legacy/sneakercare_dashboard.html` ไม่ถูกใช้งานแล้วจริงๆ
- เก็บ `sc_opex` ไว้เป็น **view read-only** อย่างน้อย 1 ปี เผื่อต้องกระทบยอดย้อนหลัง

---

## กฎระหว่างทาง

1. **ห้ามข้ามขั้น** — แต่ละขั้นต้องผ่านเทสต์ขั้น 0 ก่อนขึ้นขั้นถัดไป
2. **ห้าม migration ที่ทำลายข้อมูล** ทุกขั้นเป็น additive · rollback = หยุดใช้ตารางใหม่ ไม่ใช่ restore
3. **ห้ามแตะ `inv_stock_transactions` / `inv_audit_logs`** — append-only ตามกฎข้อ 1 และ 2
4. **ห้ามลบแถว `key='audit_log'`** ตลอดทั้งแผน — ระบบเดิมอ่านอยู่
5. ทุกขั้นที่แตะข้อมูลจริงต้องเขียน `sc_audit_logs` กำกับเหตุผลไว้เสมอ

## ประมาณเวลารวม

ขั้น 0–4 ประมาณ **3–4 วันทำงาน** (ส่วนที่ให้ผลตอบแทนสูงสุด — ปิดต้นตอบั๊กเงินได้เกือบทั้งหมด)
ขั้น 5–6 เป็นงานแยกที่รอได้ ไม่ต้องทำต่อเนื่อง
