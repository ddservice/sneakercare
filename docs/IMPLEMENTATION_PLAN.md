# แผนพัฒนาระบบให้ครบวงจร (ตรวจได้)

วันที่สำรวจ: 2026-09-19  
หลักฐานในตาราง: **โค้ด/schema** = อ่านไฟล์ใน repo · **เทสต์** = มีสคริปต์ใน `package.json` (ระบุถ้ายังไม่รันในรอบนั้น) · **เอกสารเก่า** = ข้อความในประวัติ ยังไม่นับว่าสถานะปัจจุบันถูก

ห้ามนำคำว่า “แก้แล้ว / deploy แล้ว” จาก `docs/history/` มาสรุปว่าของจริงถูกต้อง

---

## Gap Analysis

| โมดูล | สิ่งที่มีจริง (ตำแหน่ง) | สิ่งที่ยังขาด | ความเสี่ยง | งานแก้ไขและเกณฑ์ยอมรับ |
|---|---|---|---|---|
| เงินกลาง | `numeric` ใน DB · `money()` เดิมเป็น JS number · ระยะ 1 เพิ่ม `lib/money.ts` สตางค์+สตริง | caller ส่วนใหญ่ยัง `parseFloat` / `Number` · ไม่มี decimal string ทั้ง API | ยอดเพี้ยนที่ขอบ float · สูตรซ้ำคนละไฟล์ | โมดูลใหม่ผ่าน `test:money` · ผล 1000×7% = 70.00 · ไม่เปลี่ยนยอด Excel ที่ล็อกไว้จนกว่าจะย้าย caller มีเทสต์คู่ |
| VAT | `lib/vat.ts` 0 หรือ 7 · จดที่ `inv_branches.vat_registered` · คิดตอนสร้างเอกสาร | ไม่มี effective date · ไม่ snapshot ผู้ขายบนเอกสาร · ไม่แยก 0% / ยกเว้น | พิมพ์หัวบิลจากข้อมูลร้านปัจจุบัน ถ้าแก้ร้าน ใบเก่าหัวเปลี่ยน | เก็บอัตรา+ยอดบนแถวเอกสารแล้ว · หัวพิมพ์ยังสด — ต้องมีคอลัมน์ snapshot ก่อนเรียกว่าถูกต้องตามสเปก |
| WHT | `lib/wht.ts` · `sc_wht_payees` / `sc_wht_certificates` · ห้ามนับซ้ำใน opex | ไม่ผูกเอกสารซื้อ SmartAcc | ลง WHT เป็น opex ซ้ำถ้ามีคนเติมมือ | เทสต์ `test:wht` · เกณฑ์: ค่าเช่า 18000 ลง opex เต็ม WHT 900 อยู่ใบ 50 ทวิ |
| POS ขาย | `/pos` `createServiceOrder` ลง `sc_sales` เมื่อชำระแล้ว (`lib/checkout.ts`) · `/pos/daily-entry` `saveDailySale` | ไม่มีตะกร้าสินค้าสำเร็จรูป · ไม่ตัดสต๊อกตอนรับงาน · ไม่มี tendered/change | กรอกซ้ำที่ยอดขายรายวัน = นับรายได้สองครั้ง | `test:checkout` · คีย์กันซ้ำ = id ใบรับงาน · ห้ามนับ `service_orders` บน dashboard |
| รับชำระ | เงินสด/โอนบนแถวขาย · AR ที่ `sc_payments` (ผูกวันที่ ไม่ใช่ FK บิล) | ไม่มี payment line · ไม่มี state machine ภายนอก · ไม่มี idempotency | กดบันทึกซ้ำสร้างยอดซ้ำได้ | unique + idempotency key · retry ไม่สร้างแถวที่สอง |
| คืนเงิน / CN-DN | มี `VOID` และใบลดหนี้/เพิ่มหนี้ชั้นเอกสาร (`lib/smartacc/correction.ts`) | ยังไม่กลับรายการ `sc_sales` · ยังไม่คืนสต๊อก | ใช้ CN เอกสารแล้วนึกว่ากำไรเปลี่ยน | รอมติต้นทุนคืนของก่อนแตะคลัง/บัญชีร้าน |
| ลิ้นชัก | ไม่มีตาราง/หน้า | session เปิด-ปิด expected/count/variance | เงินสดในร้านกระทบยอดไม่ได้ | ledger การเคลื่อนไหว + ล็อกตอนปิด |
| คลัง | ledger append-only · moving average trigger · staff-safe view · `0044` ปฏิเสธเบิกเกิน + `FOR UPDATE` | เบิกไม่ผูกบิลขาย · เทสต์สอง session ของ Postgres จริงยังไม่มี | สต๊อกหน้าจอ 0 แต่ ledger ติดลบเชิงตรรกะถ้า trigger พัง | `test:stock-concurrency`: qty=1 สองคำสั่ง -1 สำเร็จได้หนึ่งรายการ ยอดไม่ติดลบ · PGlite คิวคำสั่ง ไม่ใช่สอง connection จริง |
| เอกสารขาย | 8 ประเภท · เลข RPC ต่อ tenant · QA/DO/INV/BL เริ่ม `DRAFT-` · `VOID` เก็บเลข · snapshot ผู้ขาย | ใบเก่าที่ออกก่อนชุดนี้ยังมีเลขตอนสร้าง | ช่องว่างเลขใบเก่าถ้าลบ | issue + เลขในก้าวเดียวสำหรับใบใหม่ |
| ลูกหนี้/เจ้าหนี้ | AR บางส่วน (`ext_billing_references`, `sc_payments`) · จ่ายผ่าน `sc_opex` | ไม่มีสมุดลูกหนี้/เจ้าหนี้เต็ม · ใบวางบิลไม่ใช่รายได้แต่ต้องกันนับซ้ำ | นับรายได้สองครั้งถ้าลงทั้ง INV และขายรายวัน | เอกสารหนึ่งเหตุการณ์รับรู้ครั้งเดียว |
| ภาษี | PND ไฟล์จากใบ WHT จริง · PP30 เป็นข้อความขาย · กระดาษทำงาน + ภาษีซื้อจาก 50 ทวิและใบเสร็จที่กรอกเอง + ปิดงวดบัญชีร้าน | สมุดซื้ออัตโนมัติ · e-Tax ส่งจริง · ภ.พ.30 ทางการ | ส่งไฟล์ตัวอย่างคิดว่ายื่นแล้ว | `readyToFile` เป็น false เสมอ จนกว่าจะมีแบบยื่นจริง |
| GL / ปิดงวด | ปิดงวดบัญชีร้านที่ `sc_settings.closed_periods` · กันแก้ `sc_sales` / `sc_payments` / `sc_opex` | ไม่มี journal / GL | ปิดงวดแล้วนึกว่าพร้อมยื่น | ปิดแล้วก็ยังไม่พร้อมยื่น · ถ้าทำ GL: เดบิต=เครดิตต่อใบ |
| พนักงาน / RBAC | `lib/permissions.ts` · `test:guards` · RLS 0031 | บางหน้าพึ่ง service_role — ต้องกรองมือ | ลืม `tenantFilter` = รั่วข้ามกิจการ | ทุก action ใหม่ผ่าน guards · เทสต์ข้าม tenant |
| หลายกิจการ/สาขา | `tenants` + `inv_branches` · VAT ที่สาขา | ไม่มีตาราง legal entity แยก · ชื่อร้านอยู่ tenant | สาขาในกิจการเดียวกันถูกบังคับคนละ VAT | รอมติเจ้าของก่อนย้าย schema |
| Dashboard | `/dashboard` เกณฑ์เงินเข้าจริง (`lib/dashboard-books.ts`) · ใบรับงานที่ชำระแล้วอยู่ใน `sc_sales` · ดึงย้อน 14 เดือน | ปุ่มย้อนหลังไม่ใช่ทั้งประวัติ · ไม่มี cache | กรอกใบรับงานซ้ำที่ยอดขายรายวัน = นับสองครั้ง | `test:dashboard` · ห้ามนับ `service_orders` · ห้ามสลับเกณฑ์เริ่มต้น |
| Audit | สองสายคลัง/แอป · append-only | denied attempts ยังไม่แยก security log | audit หายถ้า rollback ก่อน log | log ปฏิเสธสิทธิ์นอกธุรกรรมเงิน |

---

## ลำดับระยะ

ปรับจากสเปกตามหลักฐานใน repo: ความเสี่ยงเงิน/สิทธิ์มาก่อน อย่าสร้าง POS ใหม่ทับ `sc_sales` จนกว่าจะมีสะพานข้อมูล

### ระยะ 1 — อุดความเสี่ยง + สูตรเงินกลาง (เริ่มแล้ว 2026-09-19)

- ย่อเอกสารถาวร · แยกประวัติ
- `lib/money.ts` + `test:money` · ห่อ `money()` / `settleDocumentVat` ให้ใช้สตางค์ข้างใน
- **ยังไม่ทำ:** ย้ายทุก `parseFloat` · ตาราง legal entity · apply production

เกณฑ์รับ: `test:money` `test:vat` `test:wht` `test:guards` ผ่าน · ผลลัพธ์เคส 1000/18000 เท่าเดิม

### ระยะ 2 — ธุรกรรม / idempotency / กันสต๊อกเกิน (apply production 2026-09-19)

- `0044` + `test-migration-0044.mjs`: ปฏิเสธเบิกเกินก่อน trigger ถัวเฉลี่ย · อนุมัติปรับลดใช้ `fn_consume_stock_outflow` · unique `(tenant_id, client_request_id)` บน `sc_sales` / `sc_payments`
- แอปส่งคีย์จากฟอร์มยอดขายรายวัน · กดซ้ำได้ผลชุดเดียวถ้าคอลัมน์ถูก apply แล้ว
- `test:stock-concurrency`: สองคำสั่งตัดสต๊อกพร้อมกันบน qty=1 — สำเร็จได้หนึ่งรายการ ยอดสุดท้ายเป็น 0 · PGlite คิวคำสั่ง ไม่ใช่สอง session ของ Postgres จริง

เกณฑ์รับบนร้านจริง: apply `0044` หลังเจ้าของสั่ง + ของชิ้นสุดท้ายสำเร็จได้หนึ่งรายการ

### ระยะ 3 — POS–สต๊อก–รับชำระ (สะพานบัญชี 2026-09-19)

- `/pos` ที่ชำระแล้วลงแถวขายหนึ่งใบ (`client_request_id` = `service_orders.id`) · จ่ายตอนรับยังไม่ลงบัญชี
- `0045` เพิ่ม `service_order_id` สำหรับรายงาน — **apply production แล้ว 2026-09-19** · `/pos` เขียนคอลัมน์นี้ · ยอดขายรายวันไม่ใส่ · ไม่แตะแถวเก่า
- คลังยังเบิกมือที่ `/stock-out` (ไม่มีสูตรของใช้ต่องาน จึงยังไม่ตัดอัตโนมัติ)
- เกณฑ์รับสูตร: `npm run test:checkout` · `test-migration-0045.mjs`

ยังไม่ทำ: ตัดสต๊อกคู่กับขายในธุรกรรมเดียว · tendered/change

### ระยะ 4 — เอกสาร ลูกหนี้ เจ้าหนี้ (ยกเลิก + ใบลดหนี้ชั้นเอกสาร 2026-09-19)

- ลบได้เฉพาะร่างที่ยังไม่มีคนอ้าง และไม่ใช่ใบกำกับ/ใบเสร็จ/ใบลด-เพิ่มหนี้ · ที่เหลือใช้ `VOID` เก็บเลข
- ใบลดหนี้/เพิ่มหนี้อ้าง INV/TAX/REC · รวมลดหนี้ไม่เกินต้นทาง (+ DN) · ต้นทางไม่ถูกตั้ง `CONVERTED`
- เกณฑ์รับ: `npm run test:docs` — ไม่แตะ `sc_sales` และไม่คืนสต๊อก
- snapshot ผู้ขายฝังตอนสร้างเอกสาร — ใบเก่าที่ไม่มีภาพใช้ร้านปัจจุบัน
- ร่าง QA/DO/INV/BL ใช้เลข `DRAFT-` · กดออกเลขแล้วจึงกินตัวนับ · TAX/REC/CN/DN กินเลขตอนสร้าง
- **ยังไม่ทำ:** CN กลับรายการขาย/คลัง · apply/deploy

### ระยะ 5 — ภาษี reconciliation (กระดาษทำงาน + ปิดงวด 2026-09-19)

- `lib/tax-recon.ts` เทียบยอดขายร้านกับเอกสารที่ออกแล้ว · หักใบลดหนี้ · `readyToFile` เป็น false เสมอ
- เกณฑ์รับ: `npm run test:tax-recon` · `npm run test:period-close`
- ภาษีซื้อนับจากใบหัก ณ ที่จ่ายที่มี VAT + ใบเสร็จที่กรอกเอง (`lib/purchase-vat.ts`) — ไม่นับ OCR จำลอง · ยังไม่ใช่สมุดซื้อเต็ม
- ปิดงวดเก็บใน `sc_settings` · ค่าเริ่มต้นไม่ปิด · ปิดแล้วห้ามแก้บัญชีร้าน · ปิดแล้วก็ยังไม่พร้อมยื่น
- **ยังไม่ทำ:** สมุดซื้ออัตโนมัติจากทุกใบเสร็จ · e-Tax ส่งจริง · ภ.พ.30 ทางการ · GL · apply/deploy

### ระยะ 6 — Dashboard UX performance (สูตรแหล่งรายได้ 2026-09-19)

- `lib/dashboard-books.ts` ล็อกเกณฑ์เงินเข้าจริง · นับแค่ `sc_sales` + `sc_payments` (+ ค่าเช่า) · ไม่นับ `service_orders`
- แยกแหล่งใบรับงานที่ชำระแล้ว vs ยอดขายรายวันบนหน้าภาพรวม โดยไม่เปลี่ยนยอดรวม
- ดึง `sc_sales` / `sc_payments` / `sc_opex` ย้อน 14 เดือน ไม่ดึงทั้งตาราง
- เกณฑ์รับ: `npm run test:dashboard`
- **ยังไม่ทำ:** สลับเกณฑ์เริ่มต้น · cache · apply/deploy

---

## มติที่ตอบแล้ว (2026-09-19) และของที่ยังต้องอนุมัติ

ตอบจากหลักฐานใน repo แล้ว (ดูตารางใน `CLAUDE.md`):

1. สาขา ≠ นิติบุคคลโดยอัตโนมัติ · tenant = กิจการในระบบ · VAT ต่อสาขาเป็นค่าตั้ง · เลขผู้เสียภาษีคงที่ tenant
2. ยอดขายทางการ = `sc_sales` + `sc_payments` · `service_orders` เป็นใบรับงาน
3. เบิกเกินสต๊อก = ปฏิเสธที่ DB (`0044` apply แล้ว — `test:stock-concurrency` บน PGlite ผ่านแล้ว ยังไม่ใช่สอง session ของ Postgres จริง)
4. ร่างที่ไม่มีใบวางบิลอ้างลบได้ (ไม่รวมใบกำกับ/ใบเสร็จ) · ที่แปลง/ออกแล้วใช้ยกเลิก เก็บเลข · เลขที่ลบแล้วไม่คืน

ยังต้องอนุมัติก่อนลงมือ:

5. นโยบายคืนสินค้า: ต้นทุนจากบิลเดิมหรือต้นทุนปัจจุบัน?
6. อนุญาตให้ apply migration กันสต๊อกเกินบน production เมื่อใด?

---

## Dependency

```
เอกสารถาวร + lib/money
        ↓
idempotency + กันสต๊อกเกิน (อนุมัติ)
        ↓
POS เชื่อมคลัง/รับชำระ
        ↓
void / CN / snapshot เอกสาร
        ↓
รายงานภาษี + ปิดงวด
        ↓
Dashboard
```
