-- พนักงานที่ยังอยู่ระหว่างทดลองงานบางคนยังไม่ได้ขึ้นทะเบียนประกันสังคม ต้องยกเว้นไม่ให้ระบบหักประกันสังคม
-- (ลูกจ้าง 5%) ให้พนักงานกลุ่มนี้ตอนคำนวณเงินเดือน — ค่าเริ่มต้น false เพื่อไม่กระทบพนักงานเดิมที่มีอยู่แล้ว
alter table sc_employees
  add column if not exists sso_exempt boolean not null default false;
