-- ย้อน 0029 (เพิ่มค่า enum 'super_admin')
--
-- Postgres ไม่มีคำสั่งลบค่า enum ออกโดยตรง (ต้อง create type ใหม่ทั้งก้อน + แก้ทุกคอลัมน์ที่ใช้
-- type นี้ให้ชี้ไป type ใหม่ + drop type เก่า) — งานใหญ่เกินความจำเป็นสำหรับ migration ที่
-- **แค่เพิ่มชื่อเฉยๆ ไม่มีอะไรอ้างถึงมันเลย** (ไม่มี RLS/ฟังก์ชันไหนเช็ค 'super_admin' จนกว่าจะ
-- ถึง 0030) และห้ามมีบัญชีไหนตั้ง role เป็นค่านี้อยู่ก่อนจะรันไฟล์นี้ (เช็คด้วย
-- `select count(*) from profiles where role = 'super_admin'` ต้องได้ 0 ก่อนเสมอ)
--
-- ถ้าต้องการย้อนจริงๆ: ปล่อยว่างไว้เฉยๆ ปลอดภัยกว่า (ค่า enum ที่ไม่มีใครใช้ไม่มีผลอะไรกับระบบ)
-- ไม่ต้องรันอะไรเพิ่ม
